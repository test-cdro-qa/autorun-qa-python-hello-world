apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: staged_release
on:
  workflow_dispatch:
metadata:
  stages/v1alpha1:
    - name: QA_env
      jobs:
        - job1
jobs:
  job1:
    steps:
      - run: echo "hello world"
        uses: docker://golang:1.20.3-alpine3.17
        name: Hello world
        shell: sh
